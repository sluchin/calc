make
or
make debug

クライアントサーバ
 サーバでデーモン起動
  cd server
  ./calcd
 クライアントでプログラム起動
  cd client
  ./calcc

スタンドアロン
cd calc
./calcp


コーディングのコンセプト
 戻り値を返す関数は必ず戻り値チェックしてログを出力するか, プログラムを終了させるかする.
 戻り値を返す関数であえて戻り値をチェックしない関数はvoidでキャストする.
 揃えるために決まった場所に必ずコメントを入れそのうえで, できるだけ意味のあるコメントにする.





